import 'package:flutter/material.dart';

class AppIcons {
  ///loginscreen
  static const usernameicon = Icon(Icons.person, color: Colors.black);
  static const passwordicon = Icon(Icons.lock, color: Colors.black);
  static const showpasswordicon = Icon(Icons.visibility, color: Colors.black);
  static const unshowpasswordicon =
      Icon(Icons.visibility_off, color: Colors.black38);
}
